/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
module.exports = {
  images: {
    domains: [
      "fakestoreapi.com",
      "localhost",
      "images.unsplash.com",
      "res.cloudinary.com",
      "unsplash.com",
    ],
  },
};
